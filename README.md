# Pickleball Inc

PPA Tour internal AI project tracker. Static HTML dashboard — Trello-shaped backlog + current board with click-through detail per project. Tracks the two $1M targets (cost saved, revenue added).

- Live: deployed to Vercel under the `gull-stack` scope.
- Source: single `index.html` (Tailwind via CDN, vanilla JS for the detail modals).
